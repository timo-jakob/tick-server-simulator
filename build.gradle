plugins {
    id "nebula.release" version "19.0.10"
}

task buildDocker(type:Exec) {
    // build a docker file with an appropriate version
    commandLine 'docker'
    args 'build', '-t', rootProject.name + ':' + version.toString(), '.'
}